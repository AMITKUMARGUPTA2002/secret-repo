name: Recon Automation
on:
  push:
    branches: [main]

jobs:

    
    - name: Subdomain Enumeration
      run: |
        echo "Testing" > results.txt

    - name: Save results
      uses: actions/upload-artifact@v2
      with:
        name: Recon results
        path: results.txt 
